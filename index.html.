# Create a new repository on GitHub named 'MonsterKreater.github.io' # Clone the repository git clone https://github.con/monsterKreater/monsterkreater.github.io.git cd monsterkreater.github.io     # Install jekyll (assuming you have Ruby installed) gem install bundler jekyll         # Create a new Jekyll site jekyll new .       # Build the site and make it available on a local server bundle exec jekyll serve
